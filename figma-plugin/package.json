{
  "name": "colorarchive-figma-plugin",
  "version": "1.1.0",
  "description": "ColorArchive Figma plugin — browse 5,446 curated colors, apply fills, create styles, and inspect WCAG contrast",
  "scripts": {
    "build": "tsc && cp dist/code.js code.js",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.93.0",
    "typescript": "^5.3.0"
  }
}
