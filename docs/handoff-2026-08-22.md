接手 ColorArchive。**上一阶段(付费面 A 类)已全部完成、上线、并在生产端到端验证过。**
下一阶段的计划已写好并经三方评审,**但它的结论是「建议停止付费面投入」,不是「继续开发」** ——
所以开工前有一个 owner 决定要先落。

## 先读(按顺序)

1. `docs/dev-plan-2026-08-22-phase-b.md` —— **第 3 稿。§1 是头条,先读它。**
   §1 算出:08-18 计划 §4 的「≥2 笔付费」阈值**按构造就答不出来** ——
   所需转化率 1.92% 落在实测置信区间 (0%, 3.30%) 正中间,
   **做不做 B1/B2 都不会让 09-15 的判定更可信**。这一条独立于所有主观争论。
   §3 记了三方评审找到的**我的 11 个真错误**(其中 §2.5 是「我犯了我自己正在批评的那个错误」);
   §5 是三方都指向、原计划没做的事;§7.3/§7.4 记了**已知的两个盲区**。
2. `docs/paid-surface-phase-a-2026-08-18.md` —— 上一阶段做了什么、以及 §9 那批
   「我自己那批修复漏掉四处」的收尾。
3. `docs/dev-plan-2026-08-15.md` 的 **§10–§13** —— 被推翻过的东西。不读会重犯。

## 🔴 开工前必须先落的 owner 决定

计划给了两条路,**默认推荐第一条**:

- **A. 停止付费面投入,转公用事业**(§4)。**不下线 Pro**(还有一位真实订阅者),
  不删付费墙,只是不再往上面写新代码。09-15 的判定改成「记录」而非「决策」。
- **B. 仍然跑完时间盒**(§5)。那就跑最小版:**不做 B1、不做 B2**,只留三个观察项。

**两条路都要做 §6 那一件事(20 分钟):修收入口径。**
报表**现在**在说假话 —— 2026-08-21 的 digest 主题行是 `💰 1 payment`,
而那笔 ¥550 是 owner 自己的第二个邮箱(`yyyyy.yeyuhe@icloud.com`)。

## 已知的两个陷阱(第 1 稿踩过,别再踩)

- **`track("export")` 只在导出成功时发**,闸被看到/被挡住时**不发任何事件**。
  所以「0 次 export」只证明 0 次成功导出,**完全没有度量过有多少人到达了闸**。
  第 1 稿据此推出「export 闸 0 触达」,是错的。
- **`upgrade_modal_shown` 是曝光类事件,`word_paywall_hit` 要求先真的生成 5 次** ——
  两者不是同一种量,**不能直接比大小**。

## 立刻要看的一件事(时间敏感)

**`hayleyjunefry@gmail.com` 的续费在 2026-08-22 10:00 UTC** —— 全站唯一真实付费客户。
A 类的修复直接作用在这次续费上(收据邮件此前从未发出、取消不再当场收回、`past_due` 不掉 Pro)。

```
sqlite3 data.db "SELECT email,tier,subscription_status,pro_expires_at FROM users WHERE email='hayleyjunefry@gmail.com';"
sqlite3 data.db "SELECT * FROM orders WHERE datetime(created_at) >= datetime('2026-08-22');"
```

**注意范畴**:她续不续是 **retention**,不能用来证伪 acquisition(这是评审抓到的错误之一)。

## 这个仓库已经证明过的坑(全踩过)

1. **`pageviews` 表任何一列都不能当证据,包括 referrer。** 判断 URL/需求价值只有 GSC 能回答,
   入口必须 `/u/1/`(`/u/0/` 是没权限的账号)。
2. **只跑 typecheck 不够。改 hook 必须 `npx eslint <改动的文件>`**(全量 lint 会超时)。
   根布局里早返回写在 hook 之上 = 全站白屏。
3. **写完守卫要把缺陷放回去确认它真的会红,再撤回。** 这两轮所有守卫都是这么验的。
4. **验证一个实例不能推广到全部。**
5. **client 组件不能值导入带数据的模块**(会把数据集打进 bundle)。
6. **修 A 之前先问会不会弄坏 B。**
7. **别把「某个数字出现在某张表里」当成它在描述你以为的东西 —— 先读 WHERE 子句。**
   (A3 的整个前提就是这么错的:`(none)` 那条查询被 `WHERE event_name='preorder_cta_click'` 钉死。)
8. **能跳转的 301 不是死链**,别当死链改掉。

## 部署(后端改动必读)

- **scp 前必须先 diff。** droplet 靠 scp 部署、git 停在旧 commit,**两个方向都可能不一致**。
  做法:`git show HEAD:server/<f>` 存临时文件,和 droplet 的 `md5sum` 比。
- SSH **必须加** `-o IdentityAgent=none -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes -o BatchMode=yes`。
- **部署后立刻 curl `/health` 会得到 502** —— 那是 pm2 重启还没绑定 :3001 的窗口,不是崩溃。
  判据:`ss -lntp | grep 3001`。
- `better-sqlite3` 在本机(Node 26)不可用 → 带 db 的模块本地只能 `node --check`,
  真正验证在 droplet 上加 `-r dotenv/config`。
- **Lemon Squeezy 不用登后台**:`LS_API_KEY` 在 droplet `.env`,
  URL 里方括号必须写成 `%5B`/`%5D`(经 SSH 多层引号会被吃掉 → 空响应)。

## 验证命令(提交前全绿)

```
npm run typecheck          # 必须干净
npx vitest run             # 目前 741 passed
npm run test:server        # 目前 62 passed
npx eslint <改动的文件>     # 不要跑全量
npx next build             # 要构建校验就用这个,跳过 prebuild(prebuild 会生成 "xxx 5.zip" 垃圾)
git checkout next-env.d.ts # next build 会改写它
```

## 会话协议(CLAUDE.md)

开工前 `git fetch origin && git pull --rebase origin main`,读 `.claude/session-lock.json`,
按协议取锁;**一次会话一个 commit 一次 push**,推完把锁写回 null 并一起提交。

## 外部评审

`agy --print "$(cat brief.md)" --model gemini-3.1-pro-high < /dev/null`
(**模型名已含 effort,再传 `--effort` 会冲突**;**prompt 要作为 `--print` 的参数,stdin 无效**;
brief 开头写明「不要用任何工具」,否则它会去 shell 探索然后被权限拒绝)。
`codex exec --skip-git-repo-check "$(cat brief.md)" < /dev/null`(**stdin 必须关掉,否则挂住**)。
**他们会自信地说错话 —— 每一条都要自己验过再采纳**(这轮 4 条采纳、2 条部分驳回)。

## 一句话背景

这个站每月约 **500** 个 `word_generated` 会话,**历史上只有过 1 个真实付费客户**($3.47/月)。
21 天内:91 次撞付费墙 → 1 次 pro_click → **0 成交**;全站 20 个导出闸合计 **2 次 export**;
5 封访谈邀请 **5/5 送达、0 回复**。**五份独立外部评审全部判付费方向错误。**
**§7.3 说的可能才是真问题:没有人认真谈过「为什么只有 500」—— 那是分发,不是付费面。**
