# 下阶段开发计划(2026-09-01)· 把已经在卖的东西做好

> 承接 `docs/dev-plan-2026-08-31-next.md`(W1 已上线,§9 全部收口)。
> 数字全部取自 Azure 生产库与 Vercel API,只读,2026-08-31 实测。
> owner 的要求:「把目前付费的功能都做得更好一点 —— Figma、Pinterest 联动、网站自己的 exclusive 功能」。

---

## §0 🔴 先读这一节:三条硬约束,违反任何一条会毁掉正在跑的东西

### 0.1 W1 A/B 实验正在运行,到 ~2026-10-12

**不许做的事:**
- ❌ **不许改 `/guides/[slug]` 的任何渲染路径**(`guide-detail-page.tsx`、`guide-word-card.tsx`、`app/guides/[slug]/page.tsx`)
- ❌ **不许改 `word_generated` 的语义或 props**(`counted` / `surface` / `depth` 三个字段是判据的一部分)
- ❌ **不许再加任何「页面加载即发」的事件** —— 那会重演 `w1_assigned` 抬高 engagedVisits 15–20% 那一幕
  (见 `server/session-denominator.js` TRAP 4)
- ❌ **不许改 `src/lib/experiment.ts` 的分臂逻辑**,也不要复用 `ca_w1_v1` 这个 key

**判据与停止**:42 天或 589/组先到为准;`sudo node /root/ColorArchive/server/scripts/w1-readout.cjs`。
详见 `dev-plan-2026-08-31-next.md` §9.6。

### 0.2 服务器脚本改了必须手动部署 —— `git push` 不等于部署

`/root/ColorArchive` **没有 git remote**(2026-08-31 实测)。改 `server/` 下任何东西之后:

```bash
scp -o IdentityAgent=none -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes \
  server/<改过的文件> azureuser@172.207.80.109:/tmp/
ssh -o IdentityAgent=none -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes azureuser@172.207.80.109 \
  'sudo bash -c "cd /root/ColorArchive/server && cp -p <f> <f>.bak-$(date +%Y%m%d) && install -m 644 -o root -g root /tmp/<f> ./ && node --check <f> && md5sum <f>"'
```
**然后逐个 md5 与仓库比对。** 只有改了 `index.js` / 路由才需要 `pm2 restart`。

⚠️ **重启时间窗**:避开 **周一 03:00–03:59 UTC**(cache-warmer)和 **每天 09:00–09:59 UTC**
(COTD 邮件,`email-scheduler.js:193` 的小时闸 + 启动即跑一次)。

### 0.3 本机测试的正确姿势

`npm test` 全量会挂;**单文件正常**。改了 `.tsx` 至少跑这四个:

```bash
npx vitest run src/lib/__tests__/dark-mode-classes.test.ts   # dark: 同类工具冲突,会挂 CI
npx vitest run src/lib/__tests__/copy-counts.test.ts          # 文案里的数字要和真实计数一致
npx vitest run src/lib/__tests__/content-links.test.ts
npx vitest run src/lib/__tests__/retired-routes.test.ts
npm run test:server && npm run typecheck
```

---

## §1 付费面的真实状态(全部实测)

### 1.1 漏斗:瓶颈**不在**结账,在墙那一刻

近 30 天,按去重会话:

| 步骤 | 会话数 | 相对上一步 |
|---|---:|---:|
| `word_paywall_hit`(撞到免费额度上限) | **146** | — |
| 之后去看过 `/pro/` | **5** | **3.4%** |
| `word_paywall_pro_click` | 5 | — |
| `checkout_clicked` | 3 | 60% |
| **`checkout_success`** | **2** | **67%** |

**同一批 146 个撞墙会话里:登录 1 个,注册 0 个。**

→ **结账段是好的(3 次点击成交 2 笔)。断点在 146 → 5:96.6% 的人撞了墙,连 Pro 是什么都没去看。**

### 1.2 存量:7 个 Pro,3 个付过钱,**零续费**

| id | 用户 | 来源 | 状态 |
|---|---|---|---|
| 3, 4, 5 | 4 月的三个 | 无 provider、无订阅 | `pro_expires_at` 2026-08-03 **已过期,无一续费** |
| 9 | owner(icloud) | LemonSqueezy | active,¥550 |
| 25 | Hayley | LemonSqueezy | active,$3 × **2 次**(唯一续过费的外部客户) |
| 33 | James | LemonSqueezy | active,¥552 |
| 41 | 新用户 | LemonSqueezy | 🔵 **`on_trial`,2026-08-31 开,9/3 到期** |

⚠️ **`on_trial` 不是成交。** 试用到期主动取消是常态,在扣款成功前不能算收入。

✅ 权限逻辑无漏洞:`server/entitlement.js` 的 `effectiveTier()` 会降级过期 Pro,
`server/auth.js:186` 下次登录还会把 `tier` 自愈写回 `free`。那 3 行只是没再登录过。

### 1.3 Pro 现在解锁什么(9 处门,实测 grep)

| 位置 | 权益 |
|---|---|
| `export-watermark.ts:26` | **导出去水印**(免费/匿名带 `colorarchive.org` 字样) |
| `word-color-generator-page.tsx:178` | 绕过 word→color 的 5 次免费额度 |
| `palette-history-panel.tsx:24` | 调色板历史 |
| `journal-export-button.tsx:40` | 色彩日记导出 |
| `ai-usage-badge.tsx:25` | AI 额度不限(免费 10/天) |
| `me.js:37` | 项目数不限(免费 3 个) |
| `tool-upsell-banner.tsx:55` | 不再看到升级横幅 |
| `pro-gate-policy.ts:60` / `account-page.tsx:349` | 门与账户页 |

**这份清单的共同点:全部是「解除限制」,没有一项是「只有 Pro 才有的能力」。**

⚠️ **我第 1 稿据此推断「这就是零续费的结构性原因」—— 两个评审都给出了更简单的解释,已采纳,见 §2 第 2 条。**
更可能的是:**配色是项目制的一次性任务,月订阅和使用频次本身就错配**。
包装不是主因,计费形态才是。

### 1.4 Figma 插件与 Pinterest

- **Figma 插件 v1.1.0**(`figma-plugin/manifest.json`,已发布 Community):浏览 5,446 色、应用填充、
  建 paint style、品牌色阶、WCAG 检查、**Projects tab 需要接 API key**(而 API key 是账户功能)。
  **它现在完全没有 Pro 概念** —— 免费和 Pro 在插件里体验完全一样。
  ⚠️ **我第 1 稿写「插件是唯一天然的留存载体」—— 那是无数据的断言,已撤回。**
  全库只有 **7 条**与插件相关的事件,等于对它的实际使用量一无所知。见 §3 W-2 第一步。
- **Pinterest**(`server/pin-scheduler.js`):每天 1 pin。🔴 **实测 30 天带来 0 个会话**
  (同期 facebook 44、organic-search 1,467)。**它目前不是一条产出为零的管道,而是产出确实为零。** 见 §4。

---

## §2 两个模型的评审,以及我改了什么

Gemini 3.1 Pro 与 3.7 Flash 各自独立评了第 1 稿。**两边独立收敛到同样几条,而且都对。**

### ✅ 采纳(改了)

1. 🔴 **「结账段 67% → 价格不是问题」是幸存者偏差。** 两边都指出:141/146 连 `/pro/` 都没点开,
   拿穿过 98% 流失后剩下的 3 个人推断「价格没挡住人」,和在珠峰顶上问「累不累」是一回事。
   **这个推断已撤回。** 价格是否是障碍,现有数据**无法回答**。
2. 🔴 **零续费有更简单的解释,而我选了更复杂的那个。** 我写「9 项权益全是解除限制」,
   两边都给出奥卡姆版本:**配色是项目制的一次性任务** —— 用户为某个项目配一次色,导出,
   项目进入下一阶段,几个月内不会再来。**这不是包装问题,是计费模式和使用频次的错配。**
   → 方向从「加独占功能」改为「先验证一次性收费形态」(见 W-2)。
3. 🔴 **押注 Figma 插件而没有任何遥测,是在重复本项目犯过的错。** 全库只有 7 条相关事件 ——
   等于不知道插件每天有几个人打开。**W-2 降级为「先花 1 小时加心跳埋点,3 天后看数再决定」。**
4. **判据设在了最弱的一档。** 我原本写「基线 3.4%,≥10% 成立」。蒙特卡洛实测(20,000 次):

   | 真实效应 | 期望 | 检出率 |
   |---|---|---:|
   | 无效应 | 5 vs 5 | 4.4%(假阳性,正常) |
   | 2× | 5 vs 10 | 25.6% |
   | **3×(=我设的 10% 线)** | 5 vs 15 | **64.5%** |
   | 4× | 5 vs 20 | 90.8% |

   → **不是纯噪声(两边都说过头了),但 64.5% 确实不够。** 判据已改(见 W-1)。
5. **运维排序错了。** Pro 说异地备份才是真 P0。采纳,**W-3 提到最前面**。

### ❌ 不采纳(有数据,不是嘴硬)

1. **Pro 说「加大 Pinterest 引流,期望值相当且买到信号密度」。**
   → **实测 30 天 Pinterest 带来 0 个会话**(同期 facebook 44、搜索 1,467)。
   它的期望值计算假设「1→8 pin = 3 倍流量」,但**这个渠道目前的产出是零**,
   8 倍工作量乘以零还是零。**Flash 的版本反而更对:该考虑的是砍掉它,不是加码。**
2. **Pro 说「账户门槛是转化杀手」。** → 结账是 LemonSqueezy **托管页直链,买 Pro 不需要注册**
   (`checkout-button.tsx:20` redirect mode)。「0 注册」说明的是别的事(见 §1.1 注)。
3. **Flash 说「立刻杀死 W1 和一切 A/B 幻想」。** → **前提不成立。**
   W1 的约束是「不要碰 `/guides/*`」,而本阶段的战场是 `/word-to-color/` 的付费墙 —— **不重叠**。
   而且 W1 已在跑、边际成本为零、对它自己的判据有 83.8% 功效(蒙特卡洛实测)。
   杀掉它只会丢掉已经花掉的工作和正在累积的数据。
4. **Flash 说「3 个付费用户,数据库丢了手写补偿都来得及,别花 3 天做容灾」。**
   → **严重低估。** 要保的不是 3 个客户,是**整张 events 表** ——
   这个项目所有决策(含正在跑的 W1)唯一的仪器。而且修它是 1 小时不是 3 天。

---

## §3 这一阶段做什么

### 🔴 先钉死判据的量级(上一阶段连续三次栽在这)

**唯一有体量的步骤是 `word_paywall_hit` = 146 会话/月。下游全部 N<6,不可测。**
本阶段**不做 A/B**(146 分两组 = 73/组/月,且会和 W1 抢分臂机制)。

### W-0 · 立刻做,1 小时,与商业无关但不做会丢一切

🔴 **备份没有异地副本。** `crontab` 每 6 小时备份到 `/root/ColorArchive/server/backups`,
**和数据库在同一块盘上**,cron 里没有任何上传;而 memory 记着「VM 无快照」。
台账写「ColorArchive 备份已在 Azure」——那是 DO 时代的配置,**迁移后没跟过来**
(和本次会话开头发现的「脚本没部署」是同一类问题)。

Azure 存储账号 `colorarchivestu` 已存在(rg `colorarchive-backups-stu`,japaneast)。
**做法**:在现有 `backup-sqlite.sh` 末尾加一步 `az storage blob upload`,保留最近 N 份。
**判据**:能从 blob 拉回一份并 `sqlite3 ... "PRAGMA integrity_check;"` 通过。

⚠️ **注意 Flash 认为这条不值得做**(「3 个付费用户丢了手写补偿都来得及」)。
**不采纳**:要保的是整张 events 表,它是本项目唯一的仪器,而且 W1 正在往里写。

### W-1 · 主要开发项:让撞墙的人知道 Pro 是什么

**问题**:146 个人撞墙,5 个去看了 `/pro/`,**0 个注册**。墙上只写 "Free preview limit",
没有在那一刻说明「Pro 能给你什么」。

**做什么**:在墙那一刻直接呈现权益 + 一个直达结账的入口(不需要先注册,结账本来就是托管直链)。
- ❌ 不要再加「页面加载即发」的事件(§0.1)
- ❌ 不要动 `word_generated` 的任何 props

**判据(已按实测功效调整)**:`word_paywall_hit` → 同会话内 `pro_click` 或到达 `/pro%`。
基线 **5/146 = 3.4%**,前后各 30 天。
- **≥13.7%(4×,约 20/146)= 成立** —— 这一档实测检出率 90.8%
- **≤6.8%(2×)= 不成立,停手**
- 中间(6.8%–13.7%)= **判为「未证实」而不是「成立」**,因为 3× 那一档只有 64.5% 检出率

🔴 **但这一条不要当成实验来做,当成 bug 来修。** 两个模型都指出:
墙不说明权益是**可用性缺陷**,不需要等 60 天读数才决定改不改。
**先改、先上线,前后对比只是事后的合理性检查,不是发布闸门。**

⚠️ **146 这个基数会被 W1 影响**(W1 若把更多内容页读者送进工具,撞墙的人会变多)。
读数时必须**分臂看,或等 W1 结束**。这是两个实验唯一会互相污染的地方。

### W-2 · 先测量,再决定(不是先开发)

**第一步(1 小时):给 Figma 插件加一条启动心跳** —— 匿名 ping + 版本号,不带任何用户标识。
全库现在只有 7 条相关事件,等于对插件一无所知。

**3 天后看数,然后二选一:**
- **日活 > 20** → 值得做插件内的 Pro 联动(Projects tab 已经需要 API key,是天然接点)
- **日活 < 5** → **彻底放弃插件方向**,不要再往里投商业化精力

**第二步(只有第一步给出正面信号,或 W-1 成立后):试一次性收费形态。**
两个模型独立收敛到同一点:配色是一次性任务,而我们在卖月订阅。
在 LemonSqueezy 加一个 **买断 / Day Pass** SKU,和现有月订阅并列展示,看撞墙的人选哪个。
**判据**:`checkout_clicked` 的绝对数(现在 3/月)。这是唯一一个「多一倍就看得见」的数。

### W-3 · 服务以 root 运行

`node /root/ColorArchive/server/index.js` 实测以 root 跑。属既有状况,不是今天引入。
降权到独立用户是标准做法,但涉及文件属主 + PM2 配置,**要能回滚、且在低流量时段做**。
**优先级低于 W-0**:备份丢了数据没了,提权是需要先有人来打你的前提。

---

## §4 明确不做的,以及为什么

### 🔴 先说清楚判据的量级问题,免得重蹈覆辙

上一阶段连续犯了三次同一类错误(基数没钉死就乘比率,见 `dev-plan-2026-08-31-next.md` §9.7/§9.10/§9.12)。所以先钉死:

**唯一有足够体量做前后对比的步骤是 `word_paywall_hit` = 146 会话/月。**
下游全部 N<6,**任何针对结账段的改动都不可测**,不要为它们设计判据。

**并且这一阶段不做 A/B。** 理由:W1 已经占用了分臂机制(`ca_w1_v1`),
而且 146/月 分两组 = 73/组/月,要检出 3.4% → 10% 需约 250/组 = **3.4 个月**。
→ **判据一律用前后对比 + 明确的时间窗**,并接受它比 A/B 弱。

### W-1 · 主要开发项:让撞墙的人知道 Pro 是什么

**问题**:146 个人撞墙,5 个去看了 `/pro/`,0 个注册。墙本身写着 "Free preview limit",
但**没有把「Pro 能给你什么」放在那一刻**。

**做什么**(具体待定,由执行会话决定,但必须满足):
- 在墙那一刻直接呈现权益,而不是让用户自己去 `/pro/` 找
- **不要再加一个页面加载事件**(§0.1)
- 不要动 `word_generated` 的任何 props

**判据**:`word_paywall_hit` → 同会话内 `word_paywall_pro_click` 或到达 `/pro%` 的比率。
基线 **5/146 = 3.4%**。前后各取 30 天。
- 提升到 **≥10%**(约 15/146)= 成立,继续做 W-2
- **<6%** = 墙那一刻不是问题所在,停手,重估「这些人到底想不想要 Pro」

⚠️ **注意 146 这个基数本身会被 W1 影响**:W1 若把更多内容页读者送进工具,撞墙的人会变多。
**读数时必须把 W1 的两臂分开看,或等 W1 结束后再读。** 这是两个实验会互相污染的唯一一处。

### W-2 · 只有 W-1 成立才做:给 Pro 一个「持续」的理由

**问题**(§1.3):Pro 的 9 项权益全是「解除限制」,没有一项是持续产生价值的。
4 月那 3 个用户过期后**无一续费**。

**方向**(不是承诺,是候选):Figma 插件是唯一每天会被打开的表面,
而它现在对 Pro 一无所知。把「只有 Pro 才有的东西」放进用户的日常工作流,
比在网站上再加一道墙更可能带来第二个月的付费。

**判据**:这个不可测(N=3)。**用定性判据**:
先给现有 3 个付费用户发一封问「你为什么续/不续」的邮件 —— ⚠️ **需要 owner 单独授权**
(memory 记着「未发客户邮件(owner 未授权)」,这条至今有效)。

### W-3 · 运维两条(与付费无关,但今天发现,不修会一直悬着)

1. 🔴 **备份没有异地副本。** `crontab` 每 6 小时备份到 `/root/ColorArchive/server/backups`,
   **全在同一块盘上**,cron 里没有任何上传。台账写着「ColorArchive 备份已在 Azure」——
   那是 DO 时代的配置,**迁移后没跟过来**(与「脚本没部署」同一类问题)。
   Azure 存储账号 `colorarchivestu` 已存在,可直接用。
   → **这是本计划里唯一「不做会丢数据」的一条。**
2. **服务以 root 运行**(`node /root/ColorArchive/server/index.js`,实测 `ps -eo user`)。
   依赖链上任何一个路径穿越/RCE 就是 root。属于既有状况、不是今天引入的,
   但降权到独立用户是标准做法。⚠️ 改动涉及文件属主与 PM2 配置,**要在低流量时段做且必须能回滚**。

---


- ⚠️ **~~不动 Pinterest~~ —— 2026-08-31 深挖后这条被我自己推翻了一半,见 §6。**
  「0 会话」是对的,但我当时**没查过为什么**,直接跳到了「所以别碰」。查完之后有一个具体、
  便宜、可证伪的原因候选(pin 用的是 1200×630 横图,而 Pinterest 要 2:3 竖图),
  这让「修它」比「加码发更多」有意义得多。
- ❌ **不做付费墙 A/B。** 146/月分两组要 3.4 个月,而且会和 W1 抢分臂机制。
- ⚠️ **~~不做新定价 / 新 SKU~~ —— 这条被评审推翻了,已移到 W-2 第二步。**
  原文的理由(「结账段 67% 说明价格不是问题」)是幸存者偏差,见 §2。
- ❌ **不碰 `/guides/*` 的任何东西**,直到 W1 结束(§0.1)。
- ❌ **不给 iOS 加东西。** 那是另一条发布链路,而本阶段的判据全在 web 的事件表里。

---

## §5 会推翻这份计划的信号

- **W-1 上线后 `paywall_hit → pro_click` 没动** → 墙那一刻不是问题,
  真正的问题可能是「word→color 这个工具本身不值 ¥499/月」,那是产品定位问题不是转化问题
- **`word_paywall_hit` 的月量掉到 100 以下** → 唯一有体量的那一步也没了,本计划失去判据
- **W1 读数显示内容页读者根本不进工具**(<2x)→ 那么「把更多人送到墙前」这条路本身不成立
- **那个 9/3 到期的试用直接流失,且再无新试用** → 需要重新问「有没有人想为这个付钱」这个更前置的问题

---

## §6 Pinterest:它没坏,它在发,但没人点(2026-08-31 深挖)

owner 想把 Pinterest 做好。我上一轮写的是「实测 0 会话,所以不动」——
**「0 会话」成立,但「所以不动」是我没查原因就下的结论。查完之后结论变了。**

### 6.1 它一直在正常工作

`server/.pin-log.json` 有 **78 条真实发布记录**,每条都带 Pinterest 返回的 `pinId`:

| 月份 | pin 数 |
|---|---:|
| 2026-06 | 21 |
| 2026-07 | 31 |
| 2026-08 | 26 |

最新一条 `2026-08-31-color-teal-silk-bright`,`pinId 855683997996536266`,board `855684066641154147`。
`PINTEREST_SANDBOX=false`(打的是真实 API),调度器每天 02:00 UTC 发 1 条 color。

### 6.2 而它带来的访问是零 —— 那 71 条 pageview 是 owner 自己

`events` 表 90 天内 `referrer_domain` **完全没有 pinterest**。
`pageviews` 里有 71 条 pinterest/pin.it 引荐,但拆开看:

| 落地页 | 次数 | 时间 |
|---|---:|---|
| `/pinterest/callback/` | 23 | 3/22–6/10(OAuth 回调) |
| `/colors/citrine-tone-pure/` | 15 | 全在 4/07 一天 |
| `/colors/plum-tone-vivid/` | 13 | 全在 3/23 一天 |

按月:3 月 35、4 月 35、6 月 1、**7 月 0、8 月 0**。屏宽全是 1728/1566/1512 这类桌面尺寸。
→ **这些是 3–4 月配置 OAuth 时自己点出来的,不是读者。真实入站访问 = 0。**

### 6.3 🔴 最便宜的原因候选:图的比例根本不对

调度器传给 `createPin` 的是 `/colors/{slug}/opengraph-image/`(`pin-scheduler.js:182`),
**线上实测 1200×630,宽高比 1.90:1**。
而 Pinterest 建议 **2:3 竖版(1000×1500,0.67:1)** —— **当前是它的 2.9 倍宽**。
横图在瀑布流里又扁又小,曝光会被平台压得很低。

**这条的价值在于它可证伪且便宜**:做一版竖图 pin,对比前后 30 天的曝光/点击。

### 6.4 次要候选:内容形态

现在每条 pin 是**一个纯色块 + hex**。Pinterest 上被保存的是**调色板、moodboard、场景搭配** ——
单个色块几乎没有「值得收藏」的理由。站内已有 261 个 collection,天然就是调色板形态。

### 6.5 ~~🔴 但先解决这个:现在拿不到 pin 的曝光数据~~ — **这条是错的,2026-09-01 实测**

**§6.5 的前提不成立,而且方向反了:pin 级 analytics 用现有 scope 就能读,不需要 owner 重新授权。**

实测(生产 token,只读):

| 端点 | 结果 |
|---|---|
| `GET /v5/pins/{id}/analytics` | **200 ✅** |
| `GET /v5/user_account/analytics` | 401 `Missing ['user_accounts:read']` |
| `GET /v5/user_account` | 401 `Missing ['user_accounts:read']` |

Pinterest 把 **账号级** analytics 挡在 `user_accounts:read` 后面,**pin 级**走 `pins:read` ——
而这个 scope 从 2026-06-10 起就一直在 token 里。v5 里**根本不存在 `analytics:read` 这个 scope**。
所以 78 条 pin 的完整曝光史一直可读,只是**没人写过这个 fetch**。

让 owner 点一次 OAuth 只能换来账号级汇总(nice-to-have),**改变不了 §6 的任何判断**。
→ **owner 这一轮不需要做任何事。**

新增:`server/pinterest-analytics.js`(只读模块)+ `server/scripts/pin-analytics-readout.cjs`。

---

### 6.6 数据出来了:**曝光 833,保存 3,出站点击 0**

77 条可测 pin,各自从发布日算到 2026-08-30:

| 指标 | 值 |
|---|---:|
| IMPRESSION | **833** |
| SAVE | **3** |
| PIN_CLICK | 6 |
| OUTBOUND_CLICK | **0** |
| 曝光/pin | 均值 11.0,**中位数 6** |
| **首 7 天曝光/pin** | **0.05**(74 条里 71 条是 0) |
| 首 14 天曝光/pin | 1.84,**46% 是 0** |

**整个账号约 10 曝光/天。** 一条 pin 在它上线的第一周里几乎什么也拿不到 ——
这跟 Pinterest 正常的"新 pin 有一波初始分发"完全相反,说明它**根本没进推荐池**。

**不是被封也不是被降权。** 按周看 impr/pin-day 全程 0.12–0.36,**没有任何断崖**,
9/81 天全账号 0 曝光。是"从来没被检索过",不是"某天出事了"。
(这条判据现在固化在 readout 的按周表里,以后真出平台动作能一眼看出来。)

---

### 6.7 🔴 我自己写的判据错了两次,这比结论更值得记

**第 1 版**:`T≥500 → (B) 展示了但没人点`。真实数据 T=833,**直接判到 (B)**,
会把下一个月指向错误的问题。错在:500 是"预期正好出现 1 次点击"的位置,
而那恰恰是**观察到 0 次最不意外**的位置 —— 检验被建在了零功效的点上。

**第 2 版**:改成 `T>1500`(rule of three 的 3/T 降到 0.2% CTR 以下)。算术对,但没用:
- **T 是累计量**,不改进分发、只要继续发,约 **2026-11-08 自动越过 1500**。
  *靠活得久就能通过的阈值不是检验。*
- 即使到 1500,对 0.05% CTR 的功效只有 47%;要 80% 功效需要约 **3,900 曝光 ≈ 再发一年**。

**第 3 版(现行)**:**把点击率整个踢出判据**,换成两类不随时间漂移的量:
- 尺度无关的体量:**中位曝光/pin < 25**,或**固定首 14 天窗口内 0 曝光的比例 > 10%**
  (实测 6 和 46%)。注意不能用"生命期 0 曝光比例"——那 21% 主要是**年龄**造成的:
  0 曝光的 pin 平均只上线 27.9 天,非 0 的平均 48.3 天。
- **SAVE 率**,这才是一直有功效的那个:3/833 = 0.36%,`P(X≤3 | 1%) = 0.034`
  → **保存数据能在 p≈0.03 上否定"健康的 ≥1% 保存率",而点击数据什么都否定不了。**
  前两版都把 SAVE 取回来了,然后拿全组里最弱的变量去做判断。

---

### 6.8 §6.3 / §6.4 的二选一是个假分岔

计划把"图片比例"和"内容形态"写成两个分支各配一个解法。**在 Pinterest 上这两件事因果相连**:
保存是排序信号,**没人保存的创意会导致低分发**。所以 (A) 和 (B) 是同一个故障的两个观测点,
任何曝光数都分不开它们的解法。三个独立评审视角在这一点上一致。

而且比例这条的**天花板已经能算**:2–3× 提升作用在 10 曝光/天上,
即使按 1% 出站 CTR,也只有**约 5 次访问/月**。要到"每周 1 个访客"需要 ~500 曝光/周 —— **差约 50×**。

按**预期效果**(不是按成本)排序,比例是最后一名:

| 杠杆 | 状态 |
|---|---|
| 1. pin 文案可被搜索 | ✅ **本轮已做** —— 旧标题是 `Teal Silk Bright — #69F2C4`、旧描述是 `Hue 160°, saturation 84%`,都是站内生成的内部标识符,Pinterest 搜索量为 0。而对一个 0 粉丝账号,**搜索是唯一的大流量面** |
| 2. 认领域名(claim domain) | ✅ **2026-09-02 已做** —— `colorarchive.org` 已认领(`16e0cd1`)。🔴 Pinterest 的对话框**预填的是 `colorarchive.me`**,也就是本站早就迁走的域名 —— 照着点会「认领成功」却认到一个没人指向的主机名 |
| 3. 关键词化的 board | ⬜ 78 条全发在一个叫 "ColorArchive Pro" 的 board 上,产品名没有搜索意图 |
| 4. 发人们真会搜的东西(调色板) | ⬜ 见 §6.9 |
| 5. 图片比例 | ✅ **本轮已做**(顺带做,不是主力) |

认领当天顺带读到一个新数字:Pinterest 自己的 profile 页显示 **635 monthly views**。
这和 API 量出的 833 曝光/82 天 同量级,互相印证「这个账号确实几乎没有分发」。

---

### 6.9 本轮实际做了什么

| 改动 | 文件 |
|---|---|
| 1000×1500 (2:3) Pinterest 专用图,含 5 档明度色阶 | `app/colors/[slug]/pin-image/route.tsx`(新) |
| 色阶生成 + 7 条守卫测试 | `src/lib/pin-palette.ts`、`src/lib/__tests__/pin-palette.test.ts`(新) |
| pin 标题/描述改成可搜索文案 | `server/pin-scheduler.js` |
| 只读 analytics 模块 + readout | `server/pinterest-analytics.js`、`server/scripts/pin-analytics-readout.cjs`(新) |

**`app/colors/[slug]/opengraph-image.tsx` 一个字没动** —— 它同时供 OG/Twitter 卡片。

顺手修掉的真问题(全部经代码核实):

- 🔴 **`ImageResponse` 硬编码 `cache-control: public, max-age=0, must-revalidate`**
  (`next/dist/server/og/image-response.js:39`)。**next/og 不缓存**,每次请求都重跑一遍 satori。
  我第一版注释写的"Vercel CDN 会缓存"是错的,而那正是"per-colour 动态路由不贵"这个论证的支点 ——
  在一个 `/colors/*/vs/` 曾经吃掉 Vercel 账单大头的站上。已改为 `s-maxage=31536000` +
  `X-Robots-Tag: noindex`,最坏情况从"每次请求一次渲染"变成"每个 slug 一生一次,上限 5,446"。
- 🔴 **robots.txt 差点把 pin 管道押在猜对 UA 上**。我先给 `/colors/*/pin-image/` 加了 Disallow
  再给 Pinterest 开白名单 —— 但 Pinterest 是**服务端抓这个 URL** 且遵守 robots.txt,
  猜错 UA token = 每天的 pin 全部静默失败。**已撤回**;成本改用上面的缓存头解决,
  不索引改用 `X-Robots-Tag`。(这正是 `/colors/*/vs/` 那条注释学到的"noindex 要先抓才读得到"的反向应用。)
- 🔴 **`doRefresh()` 会把 owner 未来加的 scope 悄悄削回去**:它在 refresh_token 授权里硬编码
  `scope=boards:read,...`。这个陷阱**正好埋在 §6.5 原本建议的那一步下面** ——
  owner 加宽授权 → 生效 → 12 小时后(或任何一次 `pm2 restart`)被还原,日志里毫无关联线索。
  已删除该参数(RFC 6749 §6:省略即"与原授权相同"),并在 scope 变窄时报警。
- 🔴 **`pinterest-admin.init()` 在 `DISABLE_SCHEDULERS` 闸门之外**(`index.js:161` vs 闸门在 `:258`)。
  Pinterest **每次刷新都轮换 refresh token**,所以在本机起一次服务就会作废生产的凭据 ——
  而那个开关的全部承诺就是"可以安全地在笔记本上起"。已改为读 token 不设防、刷新走同一个闸。
- 🔴 **`server/.pinterest-admin-token.json` 没进 `.gitignore`**,而新模块把它解析到**仓库树内**。
  其他每个凭据文件都单独列了,只有这个没有。已补。
- **`recentlyPinnedInLog` 用无锚点子串匹配**:pin 了 `art-deco-gold-black` 会让 `art-deco-gold`
  被误判成"最近发过"并跳过 30 天。261 个 collection 里有 11 对这样的前缀关系。
  (**色号 id 之间没有前缀冲突 —— 已全量验证**,所以三个月的纯色号发布从没暴露它。)已改为匹配记录字段。
- **`"Film Emulation Palette palette"`**:标题无条件加 " palette" 后缀,而有两个 collection 标题本就以
  Palette 结尾。pin 是永久公开的。已加守卫。

---

### 6.10 判读:**2026-10-13**,判据是首 14 天曝光/pin

```bash
sudo node /root/ColorArchive/server/scripts/pin-analytics-readout.cjs
```

基线 **1.84 曝光/pin(首 14 天,n=67)**。用经验分布做的蒙特卡洛(不是 Poisson 公式 ——
实测 var/mean = 3.86,严重过散,负二项 θ=0.64):

| 提升 | 14 天 | 21 天 | 30 天 | 42 天 | 60 天 | 90 天 |
|---|---:|---:|---:|---:|---:|---:|
| 1.5× | 20% | 19% | 27% | 30% | 33% | 40% |
| **2×** | 54% | 58% | 60% | **72%** | 78% | 82% |
| **3×** | 81% | **90%** | 94% | **98%** | 98% | 100% |

⚠️ 用 Poisson 公式会算出"每组 13 条 pin 就够",**低估 4.6 倍**。这个项目已经在 A/B 的样本量上
栽过两次(见 08-25、08-31 两份计划),这次用经验分布重算了。

**预登记**:42 天(至 2026-10-13)。**能检出 3×(98%),检不出 1.5×(30%)。**
读不出提升 ≠ 没有提升,只等于"不到 3×"。

**而且必须写清楚:即便拿到 3×,也就是约 5 次访问/月。** 这一步买的是
"创意这条轴到底动不动"这个信息,**不是流量**。

### 6.11 明确没做,以及为什么

- **没开 collection pin**(§6.4 / 第三步)。三个理由,按分量:
  1. **和刚上线的图片改动同时改会让 10-13 的判读作废** —— 两个变量一起动就什么都读不出。
  2. **它不是"一个 env var"那么便宜**(评审里有一个视角这么说,是错的):`MAX_PER_DAY` 默认 1,
     `runDailyRotation` 按顺序走 ENABLED_TYPES 并在配额处 break —— 所以 `color` 吃掉当天配额,
     `collection` **永远轮不到**,日志还会打印 "reached MAX_PER_DAY=1" 看起来一切正常。
     要加就得同时设 `PIN_SCHEDULER_MAX_PER_DAY=2`(= 提高发布量,owner 明确不要),
     要替换就设成 `collection` 单独一项。
  3. **collection 的图还是 1200×630**(`/collections/{slug}/opengraph-image/`),
     就是色号 pin 刚刚搬离的那个比例。要开得先给 collection 也做一个 `pin-image` 路由。
  以上三条都已写进 `pin-scheduler.js` 的注释里,免得下次有人以为改个 env 就行。
- **没提高发布频率**(owner 明确排除)。仍是 1 条/天。
- **没碰** `/guides/[slug]` 渲染路径、`word_generated` props、`experiment.ts`;**没加任何"页面加载即发"的事件**。
- **异地备份没做** —— owner 说的触发条件是"如果 Pinterest 卡在等授权就先做这个",
  而 Pinterest 并没有卡住(见 §6.5)。它仍然是悬着的 P0。

---

## §7 AI 助手引荐:第三大渠道,但它现在**不转化** —— 而 W1 正好就是那个干预(2026-09-03 实测)

### 7.1 我上一轮的判断错了,这里更正

我看到 `22 → 224` 的月度增长,说「这是真正在长的渠道,应该往这儿投」。**查完转化,结论反过来了。**

60 天,按渠道:

| 渠道 | 会话 | 到达工具页 | 撞付费墙 | 点 Pro | 结账 |
|---|---:|---:|---:|---:|---:|
| **AI 助手** | **268** | **7 (2.6%)** | 2 | **0** | **0** |
| organic-search | 1,883 | **887 (47%)** | 165 | 9 | **3** |
| direct | 1,341 | 126 (9.4%) | 23 | 0 | 0 |
| facebook | 45 | 0 | 0 | 0 | 0 |

**AI 流量在「到达工具」这一步比自然搜索差 18 倍,60 天产生 0 个付费意向。**
全站仅有的 3 笔结账**全部来自 organic-search**。

### 7.2 原因在落地页,一眼可见

| 渠道 | 落地页 Top 1 |
|---|---|
| AI 助手 | **`/guides/*` 154/268 = 57%** |
| organic-search | **`/word-to-color/` 846/1,883 = 45%** |

**自然搜索带来的是「找工具的人」,直接落在工具上;AI 助手引用的是「文章」。**
AI 流量 = 内容页流量,它从来没跨到工具那一侧。

### 7.3 所以正确的动作是「什么都别新建」

那道跨越正是 **W1 正在测的东西**(guides 内容页 → 工具卡片,判读 2026-10-12)。

所以「AI 引荐该怎么办」的答案**不是**去写 `llms.txt` 或改 guides —— 那既为时过早,
**也违反 §0.1**(不许动 guides 渲染路径)。答案是:**已经在跑的实验就是那个干预**,
只要让它的判读顺便回答这个问题。成本 = `w1-readout.cjs` 里一个只读的分段,已加。

⚠️ **该分段先天功效不足**(AI guides 会话约 77/月,两臂分完每臂个位数)。
**只能当方向,不能当结论。** 若卡片对 AI 读者有任何提升,那是「把卡片扩到 `/colors/*`」的论据
—— 因为剩下的引用流量落在那儿 —— 而不是一个独立结论。

### 7.4 顺带:成本那条已结构性关闭

`/colors/{a}/vs/{b}/` 现在返回 **308 + 15 字节**,不再渲染 → **ISR 写入在机制上不可能再发生**,
与爬虫行为无关。三条独立佐证:路由已删、robots 仍在挡、pageviews 从 8,583/天 掉到 ~2/天。
(总账单仍未核实 —— 金额只在网页上,而 1Password 中继当时不可用。)
