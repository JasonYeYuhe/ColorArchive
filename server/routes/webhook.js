const express = require("express");
const router = express.Router();

// Lemon Squeezy webhook has been removed — all commerce now handled via
// Stripe Checkout with webhook at the Next.js API route (/api/webhook).

module.exports = router;
